{
  "name": "wizdom",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin && npm install --prefix app",
    "dev": "concurrently -n backend,admin,app -c blue,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix admin\" \"npm start --prefix app\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin",
    "dev:app": "npm start --prefix app"
  },
  "dependencies": {
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
